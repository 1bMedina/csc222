#include <string>
#include <vector>
using namespace std;

string render_num_vector(const vector<int>&);
string render_num_vector_d(const vector<double>&);
int num_vector_sum(const vector<int>&);
int num_vector_product(const vector<int>&);
vector <int> only_evens(const vector<int>&);
vector<int> only_odds(const vector<int>&);
vector<int> nums_between(const vector<int>&, int nums1, int nums2);
vector<double> mean(const vector<double>&);
